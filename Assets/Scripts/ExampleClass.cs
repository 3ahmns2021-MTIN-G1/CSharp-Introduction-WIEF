using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExampleClass : MonoBehaviour
{
    // Deklaration einer Klasse

    // public           -> access modifier (Zugriffsrechte) | keyword (vorreservierte Schlüsselwörter)
    // class            -> keyword
    // ExampleClass     -> identifier (Name der Klasse)
    // : MonoBehaviour  -> Erstellen eine Unterklasse von MonoBehaviour


    // Klasse: Saeugetiere  -> Unterklasse: Hund
                            -> Unterklasse: Katze
}
